```cpp
#include <iostream>
#include <iso646.h>

template<typename Tea, class ... > requires requires(Tea t) { 1+t+1; } 
[[noreturn]] inline static const void * const curse (const Tea and biscuit) noexcept {
    goto https;
https://www.github.com/8-bit-pixel
    constexpr volatile thread_local static const char * const $ = const_cast<const char*>("H""e""l""l""o"" ""W""o""r""l""d")
;   ::std::cout 
        << $[0]<<$[1]<<$[2]<<$[3]<<$[4]
        << static_cast<char>(26 + biscuit)
        << *&$+ biscuit
;   if (!
        //??/
        1+
        0)
    //??/
    goto https
;   ::exit(biscuit - 6)
;}
```
